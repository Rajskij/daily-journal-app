<html lang="en">
<%- include('./partial/head.ejs', { pageName : 'About'}) %>
    <body>
        <%- include('./partial/nav.ejs', { page: 'users'})%>
        <div class="container mt-4">
            <h3>Create User</h3>
            <form action="/users" method="post">
                <div class="form-row">
                    <div class="form-group col-md-4">
                        <label for="name"><h5>Name</h5></label>
                        <input name="name" class="form-control" id="name" rows="3" placeholder="Enter user name"></textarea>
                    </div>
                    <div class="form-group col-md-4">
                        <label for="wether"><h5>Email</h5></label>
                        <input type="email" name="email" class="form-control" id="email" rows="3"  placeholder="Enter user email"></textarea>
                    </div>
                </div>
                <button type="submit" class="btn btn-secondary">Submit</button>
            </form>
            <h3 class="mt-5">Users</h3>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table">
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(user => { %>
                            <tr>
                                <td><%= user.id %></td>
                                <td><%= user.name %></td>
                                <td><%= user.email %></td>
                                <td>
                                    <button class="btn btn-sm delete-btn" name="users" data-id="<%= user.id %>">
                                        <i class="bi bi-trash"></i> DELETE
                                    </button>
                                </td>
                            </tr>
                        <% }) %> 
                    </tbody>
                </table>
            </div>
        </div>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
        <script src="/handler/eventHandler.js"></script>
    </body>
</html>